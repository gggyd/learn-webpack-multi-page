'use strict';

require('../css/lib/reset.css');
require('../css/list.css');

var app = app || {};

app = (function(){
  var init = function() {
    // console.log('init');
  }

  return {
    init: init
  }
}());

app.init();